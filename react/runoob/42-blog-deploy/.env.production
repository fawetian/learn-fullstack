# 42-blog-deploy: 生产环境变量
# 来源章节: https://www.runoob.com/react/react-blog-deploy-vercel.html
#
# 这是 React 的生产环境变量文件，在运行 vite build 时自动加载并覆盖 .env 中的同名变量。
# 需要注意：生产环境的 API 地址通常与开发环境不同，应指向真实的后端服务。

# API 基础地址（生产环境，替换为真实后端地址）
VITE_API_BASE_URL=https://your-production-api.com

# 应用名称
VITE_APP_NAME=React 博客
